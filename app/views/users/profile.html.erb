<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style type="text/css">
      html, body{ margin:0; height:100%; }
      .input{
        margin-bottom: 20px;
      }
      .group_div{
        margin-left: 10%;
        margin-right: 10%;
      }
      .head_img{
        width: 180px;
        height: 180px;
        margin-bottom: 20px;
      }
      .btn{
        width: 80%;
      }
      .alert{
        text-align: center;
      }
    </style>
	</head>
	<body>
    <nav class="nav navbar-default navbar-static-top" style="background-color: #000000;">
      <div class="navbar-header">
         <button type="button" class="navbar-toggle" data-toggle="collapse"  data-target="#sidebar">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
         </button>
         <a class="navbar-brand" href="#">Sencetime科技展</a>
      </div>
      <div class="collapse navbar-collapse" id="sidebar">
        <ul class="nav navbar-nav">
          <li><a href="#">个人信息</a></li>
          <li><a href="<%= new_user_path %>">编辑信息</a></li>
          <li><a href="<%= records_path %>">所有伙伴</a></li>
        </ul>
      </div> 
    </nav>
  
    <div class="container">
      <!-- 操作提示信息 -->
      <% flash.each do |message_type, message| %>
        <%= content_tag(:div, message, class: "alert alert-#{message_type}") %> 
      <% end %>

      <div class="row">
        <div class="col-xs-offset-3">
          <img class="img-circle img-responsive head_img" src='<%= image_path("demo_image_file.jpg") %>'>
        </div>
      </div>
      <br>
      <div class="row">
        <div class='group_div'>
          <div class="input-group input">
            <span class="input-group-addon">
              <span class="glyphicon glyphicon-user"></span> 姓名
            </span>
            <input type="text" name="name" class="form-control" value="<%= @user.try(:name) %>" disabled="">
          </div>
        </div>
        <div class="group_div">
          <div class="input-group input">
            <span class="input-group-addon">
              <span class="glyphicon glyphicon-phone"></span> 手机                
            </span>
            <input type="text" name="phone" class="form-control" value="<%= @user.try(:phone) %>" disabled>
          </div>
        </div>
          <div class="group_div">
            <div class="input-group input">
              <span class="input-group-addon">
                <span class="glyphicon glyphicon-info-sign"></span> 职位                
              </span>
            <input type="text" name="profession" class="form-control" value="<%= @user.try(:profession) %>" disabled>
          </div>
        </div>
      </div>
    </div>

	</body>
</html>